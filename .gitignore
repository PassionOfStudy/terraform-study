# Terraform files
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# AWS MFA 설정 파일 (민감한 정보 포함)
aws-mfa-config.sh

# 환경 변수 파일
.env
.env.local

# Terraform 변수 파일
# terraform.tfvars는 기본값만 포함되어 있어 Git에 커밋합니다
# 민감한 정보가 포함될 경우 아래 주석을 해제하여 제외할 수 있습니다
# terraform.tfvars

# IDE files
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

